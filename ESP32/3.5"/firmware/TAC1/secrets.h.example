#pragma once
// TAC-1 — copia este archivo a "secrets.h" y rellena. secrets.h esta en
// .gitignore: nunca se sube ni se pega en un chat.
//
// La red se puede capturar desde la pantalla (Inicio -> toca el estado de
// red); lo de aqui solo SIEMBRA la primera vez, cuando la NVS esta vacia.
#define WIFI_SSID        "TU_RED_WIFI"
#define WIFI_PASSWORD    "TU_PASSWORD_WIFI"

// Monterrey: CST6 todo el ano, sin horario de verano.
#define NTP_SERVER       "pool.ntp.org"
#define TZ_INFO          "CST6"

// Clima (Open-Meteo, sin API key): Monterrey
#define WEATHER_LAT      25.6866
#define WEATHER_LON      -100.3161
